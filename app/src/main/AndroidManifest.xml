<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hackfrostletsgo">

    <uses-permission android:name="android.permission.INTERNET" />                     <!-- Permission for internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>        <!-- Permission for accessing storage -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.HackfrostLetsGo">
        <activity
            android:name=".activitysettings"
            android:exported="false" />
        <activity
            android:name=".Uploader"
            android:exported="false" />
        <activity
            android:name=".Firebase"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HackfrostLetsGo.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>